
@{
    ViewData["Title"] = "CustomerCastExemple";
    Layout = "~/Views/Layout/Index.cshtml";
}

<h1>Müşteri Cast Örneği</h1>
@foreach (var item in ((IEnumerable<object>)ViewBag.v).Cast<Customer>())
{
    <p>@item.Name @item.Surname - @item.City</p>
}